// The Google Calendar OAuth sync feature has been removed (edge functions,
// DB tables and all app UI are gone too). Nothing imports this module
// anymore. It's kept as an empty file — rather than deleted — only because
// this session's connection to your PC has no file-delete capability on
// Windows. Safe to delete src/lib/googleCalendar.ts by hand.
export {};
