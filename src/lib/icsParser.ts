// The .ics calendar import feature has been removed (its screen and route
// are gone too). Nothing imports this module anymore. It's kept as an empty
// file — rather than deleted — only because this session's connection to
// your PC has no file-delete capability on Windows. Safe to delete
// src/lib/icsParser.ts by hand.
export {};
